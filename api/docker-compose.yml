services:
  naijatech-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ../Yel-JSON:/app/Yel-JSON:ro
      - ../prompts:/app/prompts:ro
    restart: unless-stopped
